## [0.12.0] - 2026-02-12

### 🚀 Features

- *(icons)* Add microsoft icon, it is deprecated in mdi and will be removed from the library later
- *(buttons)* Add toggle button icon only variant, add to demos
- *(styles)* Add bg svg pattern styles
- *(generate-image-list)* Add image list generation
- *(feature-grid)* Add feature grid section, useful for homepage
- *(card)* Add card wrapper component
- *(sidebar)* Add sidebar component, wip
- *(env)* Add missing, make fallback explicit/configurable
- *(icons)* Add dev.to and bluesky icons
- *(icons)* Add apple and google play

### 🐛 Bug Fixes

- *(styles)* Fix some styles break because of semicolon
- *(feature-grid)* Add technical features
- *(demo-table)* Use CovuDialog instead of NativeDialog
- *(demos)* Add/update list, add app in path
- *(_headers)* Move _headers to public/ for Cloudflare Pages
- *(images)* Move images dir, serve images from cf deployment instead of b2 storage, add caching for images, remove by typed img variant

### 🚜 Refactor

- *(formMode)* Add title function for buttons
- *(demos)* Move demoList to app dir
- *(icon)* Make path single tag
- *(theme)* Move handler function to the bottom

### 🎨 Styling

- *(demo-styles)* Improve style demo
- *(nav)* Better align

### ⚙️ Miscellaneous Tasks

- Chore(migrate): migrate from adaptive-shield-matrix -> david1gp, rename
adaptive-sm -> adaptive-ds
